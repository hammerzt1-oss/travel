# Render连接GitHub指南

## 🔗 连接GitHub到Render

### Step 1：连接GitHub账号（2分钟）

1. **在Render页面找到GitHub选项**
   - 你应该看到 "GitHub" 选项
   - 点击 "Connect" 或 "Configure"

2. **授权Render访问GitHub**
   - 会跳转到GitHub授权页面
   - 点击 "Authorize Render" 或 "授权"
   - 允许Render访问你的仓库

3. **选择仓库权限**
   - 可以选择 "All repositories"（所有仓库）
   - 或选择 "Only select repositories"（仅选定的仓库）
   - 推荐：选择 "Only select repositories"，然后选择你的 `travel` 仓库

4. **完成授权**
   - 点击 "Install" 或 "安装"
   - 等待授权完成

### Step 2：刷新Render页面（1分钟）

1. **返回Render Dashboard**
   - 授权完成后，会自动返回Render
   - 或手动刷新页面

2. **验证连接**
   - 应该能看到你的GitHub仓库列表
   - 找到 `travel` 仓库

### Step 3：继续部署（如果已连接）

如果已经连接成功，继续以下步骤：

1. **创建Web Service**
   - 点击 "New" → "Web Service"
   - 选择你的 `travel` 仓库
   - 点击 "Connect"

2. **配置服务**
   - Name: `travel-backend`
   - Environment: `Node`
   - Root Directory: `backend` ⚠️ **重要**
   - Build Command: `cd backend && npm install`
   - Start Command: `cd backend && npm start`

3. **配置环境变量**
   - 添加 `OTA_PID=284116645`
   - 添加 `NODE_ENV=production`

4. **部署**
   - 点击 "Create Web Service"
   - 等待部署完成

---

## ⚠️ 如果还是看不到仓库

### 检查清单

1. **确认GitHub账号**
   - 确保你登录的是正确的GitHub账号
   - 确保 `travel` 仓库在这个账号下

2. **检查仓库权限**
   - 确保仓库是公开的（Public）
   - 或确保你授权了私有仓库访问

3. **重新授权**
   - 在Render设置中，断开GitHub连接
   - 重新连接GitHub
   - 重新授权

4. **检查仓库名称**
   - 确认仓库名称是 `travel`
   - 或确认仓库名称正确

---

## 🔧 替代方案

如果GitHub连接有问题，可以尝试：

### 方案1：使用Railway（更简单）

Railway的GitHub连接通常更顺畅：
1. 访问 https://railway.app
2. 使用GitHub登录
3. 自动连接仓库

### 方案2：手动部署（如果仓库连接失败）

如果GitHub连接一直失败，可以考虑：
1. 使用Railway（推荐）
2. 或联系Render客服

---

## 📝 下一步

**连接成功后：**
1. 选择 `travel` 仓库
2. 创建Web Service
3. 按照 `backend/部署执行清单.md` 继续部署

---

**预计时间：** 5-10分钟
**状态：** 等待GitHub连接

