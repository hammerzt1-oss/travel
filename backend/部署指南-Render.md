# Render部署指南

## 🚀 快速部署步骤

### Step 1：创建账号（2分钟）

1. 访问 https://render.com
2. 点击 "Get Started for Free"
3. 使用GitHub账号登录
4. 授权Render访问GitHub仓库

### Step 2：创建Web Service（5分钟）

1. 点击 "New" → "Web Service"
2. 选择项目仓库（travel）
3. 点击 "Connect"

### Step 3：配置服务（5分钟）

**重要配置：**

- **Name：** `travel-backend`
- **Environment：** `Node`
- **Region：** 选择最近的区域（如：Singapore）
- **Branch：** `main` 或 `master`
- **Root Directory：** `backend` ⚠️ **必须设置**
- **Build Command：** `cd backend && npm install`
- **Start Command：** `cd backend && npm start`

### Step 4：配置环境变量（5分钟）

1. 在配置页面找到 "Environment Variables"
2. 点击 "Add Environment Variable"
3. 添加以下变量：

```
Key: OTA_PID
Value: 284116645
```

```
Key: NODE_ENV
Value: production
```

### Step 5：部署（3-5分钟）

1. 点击 "Create Web Service"
2. 等待部署完成（约3-5分钟）
3. 查看部署日志，确认无错误
4. 获取部署URL（如：`https://travel-backend.onrender.com`）

### Step 6：验证部署（2分钟）

1. 访问健康检查：`https://你的URL/health`
2. 应该返回：
```json
{
  "status": "ok",
  "timestamp": "...",
  "service": "travel-backend",
  "version": "1.0.0"
}
```

3. 测试API：`https://你的URL/api/recommendations?type=week`
4. 应该返回推荐列表数据

---

## ✅ 验证清单

- [ ] 部署成功
- [ ] 健康检查通过（/health返回200）
- [ ] API正常响应
- [ ] 环境变量配置正确
- [ ] OTA链接生成正常

---

## 🔧 故障排查

### 问题1：部署失败

**检查：**
- Root Directory是否正确（应该是 `backend`）
- Build Command是否正确
- Start Command是否正确

### 问题2：服务无法启动

**检查：**
- 环境变量是否正确配置
- 日志中是否有错误信息
- package.json是否存在

### 问题3：健康检查失败

**检查：**
- 服务是否正常运行
- 日志中是否有错误
- 网络连接是否正常

---

## ⚠️ Render免费套餐限制

- **服务休眠：** 15分钟无活动后服务会休眠
- **首次访问：** 休眠后首次访问需要等待30-60秒唤醒
- **建议：** 可以使用Uptime Robot等工具定期ping服务，保持活跃

---

## 📝 部署后操作

1. **获取部署URL**
   - 在Render服务页面找到URL
   - 复制URL（如：`https://travel-backend.onrender.com`）

2. **通知前端工程师**
   - 提供后端URL
   - 前端需要配置 `NEXT_PUBLIC_API_URL`

3. **验证功能**
   - 测试所有API端点
   - 验证OTA链接生成
   - 检查日志

---

**预计时间：** 20-30分钟
**状态：** 就绪

